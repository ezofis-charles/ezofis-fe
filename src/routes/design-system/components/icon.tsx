import { createFileRoute } from '@tanstack/react-router'
import { IconStory } from '@/stories/design-system/components/icon'

export const Route = createFileRoute('/design-system/components/icon')({
  component: IconStory,
})
