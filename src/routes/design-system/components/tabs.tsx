import { createFileRoute } from '@tanstack/react-router'
import { TabsStory } from '@/stories/design-system/components/tabs'

export const Route = createFileRoute('/design-system/components/tabs')({
  component: TabsStory,
})
